#ifndef ALGORITMOS_H
#define ALGORITMOS_H

#include <vector>

int busquedaLineal(const std::vector<int>& arreglo, int objetivo);

int busquedaBinaria(const std::vector<int>& arreglo, int objetivo);

int busquedaTrinaria(const std::vector<int>& arreglo, int objetivo);

#endif // ALGORITMOS_H